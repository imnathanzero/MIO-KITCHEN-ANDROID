name: Android Build

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: Build variant
        required: true
        default: release
        type: choice
        options:
          - debug
          - release
          - release_mini
          - all

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.build_type }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        build_type: ${{ fromJSON(inputs.build_type == 'all' && '["debug","release","release_mini"]' || format('["{0}"]', inputs.build_type)) }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17 for Android SDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: sdkmanager "platforms;android-28" "build-tools;28.0.3"

      - name: Set up Java 8 for Gradle build
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '8'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Restore Android signing keystore
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          test -n "$ANDROID_KEYSTORE_BASE64" || {
            echo "ANDROID_KEYSTORE_BASE64 repository secret is required" >&2
            exit 1
          }
          printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/release.keystore"
          chmod 600 "$RUNNER_TEMP/release.keystore"

      - name: Build APK
        run: ./gradlew --no-daemon --stacktrace "assemble${{ matrix.build_type == 'debug' && 'Debug' || matrix.build_type == 'release' && 'Release' || 'Release_mini' }}"

      - name: Locate exact APK
        id: apk
        shell: bash
        run: |
          case "${{ matrix.build_type }}" in
            debug) expected="pio/build/outputs/apk/debug/pio-debug.apk" ;;
            release) expected="pio/build/outputs/apk/release/pio-release-unsigned.apk" ;;
            release_mini) expected="pio/build/outputs/apk/release_mini/pio-release_mini-unsigned.apk" ;;
          esac
          test -f "$expected" || { echo "Expected APK not found: $expected" >&2; find pio/build/outputs/apk -type f -name '*.apk' -print >&2; exit 1; }
          echo "path=$expected" >> "$GITHUB_OUTPUT"
          echo "APK: $expected"

      - name: Sign APK with release keystore
        env:
          KEYSTORE: ${{ runner.temp }}/release.keystore
          STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          apksigner="${ANDROID_HOME}/build-tools/28.0.3/apksigner"
          test -x "$apksigner" || { echo "apksigner not found: $apksigner" >&2; exit 1; }
          "$apksigner" sign \
            --ks "$KEYSTORE" \
            --ks-pass "pass:$STORE_PASSWORD" \
            --ks-key-alias "$KEY_ALIAS" \
            --key-pass "pass:$KEY_PASSWORD" \
            --out "$RUNNER_TEMP/mio-kitchen-${{ matrix.build_type }}-signed.apk" \
            "${{ steps.apk.outputs.path }}"
          "$apksigner" verify --verbose "$RUNNER_TEMP/mio-kitchen-${{ matrix.build_type }}-signed.apk"
          echo "Verified signed APK"

      - name: Upload signed APK to GoFile
        id: gofile
        uses: ahnafnafee/action-upload-gofile@v3.0.0
        with:
          token: ${{ secrets.GOFILE_TOKEN }}
          file: ${{ runner.temp }}/mio-kitchen-${{ matrix.build_type }}-signed.apk

      - name: Add download link to job summary
        env:
          GOFILE_URL: ${{ steps.gofile.outputs.url }}
        run: |
          {
            echo "## Android build: \`${{ matrix.build_type }}\`"
            echo
            echo "**Signed APK:** \`mio-kitchen-${{ matrix.build_type }}-signed.apk\`"
            echo
            echo "### Download"
            echo "[Download APK]($GOFILE_URL)"
          } >> "$GITHUB_STEP_SUMMARY"
